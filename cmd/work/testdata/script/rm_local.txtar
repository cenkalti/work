# work rm -y <name> from inside a repo removes the worktree and deletes the branch.

mkdir projects/repo
cd projects/repo
exec git init -q
exec git commit --allow-empty -q -m init

work mk foo
exists .work/tree/foo

work rm -y foo

! exists .work/tree/foo
exec git branch --list foo
! stdout foo
